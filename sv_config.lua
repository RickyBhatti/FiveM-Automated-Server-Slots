Config = {
    startingSlots = 1, -- The starting amount of slots the server has.
    maxSlots = 32, -- The maximum amount of slots the server can have.
    slotIncrement = 8 -- The amount of slots to add or remove when the player count reaches the current slot limit.
}